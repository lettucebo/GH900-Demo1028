@page
@model ExamplesModel
@{
    ViewData["Title"] = "程式碼範例";
}

<h1>@ViewData["Title"]</h1>

<div class="container">
    <p class="lead">以下是一些使用 .NET 9 和 Razor Pages 的實用範例</p>

    <div class="row mt-4">
        <div class="col-md-6">
            <div class="card">
                <div class="card-header bg-primary text-white">
                    <h5 class="mb-0">範例 1：顯示當前時間</h5>
                </div>
                <div class="card-body">
                    <p>使用 C# 程式碼在頁面上顯示當前時間：</p>
                    <div class="alert alert-success">
                        <strong>當前時間：</strong>@Model.CurrentTime
                    </div>
                    <pre><code>public string CurrentTime { get; set; }

public void OnGet()
{
    CurrentTime = DateTime.Now.ToString("yyyy-MM-dd HH:mm:ss");
}</code></pre>
                </div>
            </div>
        </div>

        <div class="col-md-6">
            <div class="card">
                <div class="card-header bg-success text-white">
                    <h5 class="mb-0">範例 2：隨機數字產生器</h5>
                </div>
                <div class="card-body">
                    <p>使用 Random.Shared 產生隨機數字：</p>
                    <div class="alert alert-info">
                        <strong>隨機數字：</strong>@Model.RandomNumber
                    </div>
                    <pre><code>public int RandomNumber { get; set; }

public void OnGet()
{
    RandomNumber = Random.Shared.Next(1, 101);
}</code></pre>
                    <p class="mt-2"><small class="text-muted">重新載入頁面以獲得新的隨機數字</small></p>
                </div>
            </div>
        </div>
    </div>

    <div class="row mt-4">
        <div class="col-md-12">
            <div class="card">
                <div class="card-header bg-info text-white">
                    <h5 class="mb-0">範例 3：迴圈與集合</h5>
                </div>
                <div class="card-body">
                    <p>使用 Razor 語法顯示集合資料：</p>
                    <h6>.NET 的主要特色：</h6>
                    <ul class="list-group">
                        @foreach (var feature in Model.DotNetFeatures)
                        {
                            <li class="list-group-item">
                                <i class="bi bi-check-circle-fill text-success"></i> @feature
                            </li>
                        }
                    </ul>
                    <div class="mt-3">
                        <pre><code>@@foreach (var feature in Model.DotNetFeatures)
{
    &lt;li&gt;@@feature&lt;/li&gt;
}</code></pre>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <div class="row mt-4">
        <div class="col-md-12">
            <div class="card">
                <div class="card-header bg-warning">
                    <h5 class="mb-0">範例 4：條件式渲染</h5>
                </div>
                <div class="card-body">
                    <p>根據條件顯示不同的內容：</p>
                    @if (Model.RandomNumber > 50)
                    {
                        <div class="alert alert-success">
                            <strong>恭喜！</strong>您的隨機數字 (@Model.RandomNumber) 大於 50！
                        </div>
                    }
                    else
                    {
                        <div class="alert alert-warning">
                            <strong>注意：</strong>您的隨機數字 (@Model.RandomNumber) 小於或等於 50。
                        </div>
                    }
                    <pre><code>@@if (Model.RandomNumber &gt; 50)
{
    &lt;div&gt;數字大於 50&lt;/div&gt;
}
else
{
    &lt;div&gt;數字小於或等於 50&lt;/div&gt;
}</code></pre>
                </div>
            </div>
        </div>
    </div>

    <div class="row mt-4 mb-4">
        <div class="col-md-12">
            <div class="card">
                <div class="card-header bg-secondary text-white">
                    <h5 class="mb-0">範例 5：環境資訊</h5>
                </div>
                <div class="card-body">
                    <p>顯示應用程式的執行環境資訊：</p>
                    <table class="table table-striped">
                        <tbody>
                            <tr>
                                <td><strong>.NET 版本</strong></td>
                                <td>@Environment.Version</td>
                            </tr>
                            <tr>
                                <td><strong>作業系統</strong></td>
                                <td>@Environment.OSVersion</td>
                            </tr>
                            <tr>
                                <td><strong>處理器數量</strong></td>
                                <td>@Environment.ProcessorCount</td>
                            </tr>
                            <tr>
                                <td><strong>電腦名稱</strong></td>
                                <td>@Environment.MachineName</td>
                            </tr>
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>
